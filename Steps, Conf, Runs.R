#Setting these so theyre the same across all IRFs
steps <- 28
conf <- .95
runs <- 1000
var.names.main.graph <- c("GDP", "RES", "DEF", "INT", "HOU")
var.names.assets.graph<- c("GDP", "RES", "DEF", "ASS", "HOU")